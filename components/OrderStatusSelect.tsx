"use client";
import { useState } from "react";
const statuses = ["new", "confirmed", "roasting", "preparing", "ready_for_pickup", "shipped", "completed", "cancelled"];
export function OrderStatusSelect({ id, initial }: { id: string; initial: string }) { const [status, setStatus] = useState(initial); const update = async (value: string) => { setStatus(value); await fetch("/api/admin/orders", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id, order_status: value }) }); }; return <select value={status} onChange={(event) => void update(event.target.value)} className="border border-[#bdb3a5] bg-transparent p-2 text-xs uppercase"><option disabled>{initial}</option>{statuses.map((item) => <option key={item}>{item}</option>)}</select>; }