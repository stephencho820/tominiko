import { createClient } from "@/lib/supabase/server";
import { OrderStatusSelect } from "@/components/OrderStatusSelect";
export default async function AdminOrders() { const supabase = await createClient(); const { data: orders } = await supabase.from("orders").select("*").order("created_at", { ascending: false }); return <main className="px-6 py-12 md:px-12"><p className="eyebrow">Fulfillment</p><h1 className="mt-3 text-5xl">Orders</h1><div className="mt-10 overflow-x-auto border-y border-[#c8bfb2]"><table className="w-full min-w-[820px] text-left"><thead className="eyebrow"><tr><th className="py-4">Order</th><th>Date</th><th>Customer</th><th>Type</th><th>Total</th><th>Payment</th><th>Order status</th></tr></thead><tbody>{(orders ?? []).map((order) => <tr className="border-t border-[#c8bfb2]" key={order.id}><td className="py-4">{order.order_number}</td><td>{new Date(order.created_at).toLocaleDateString()}</td><td>{order.customer_name}</td><td>{order.fulfillment_type}</td><td>₩{order.total.toLocaleString()}</td><td><span className={`eyebrow ${order.payment_status === "paid" ? "text-green-800" : "text-[#8a3928]"}`}>{order.payment_status}</span></td><td><OrderStatusSelect id={order.id} initial={order.order_status} /></td></tr>)}</tbody></table></div></main>; }
