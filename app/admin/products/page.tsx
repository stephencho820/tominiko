import Link from "next/link";
import { getProducts } from "@/services/products";
import { ProductDeleteButton } from "@/components/ProductDeleteButton";
export default async function AdminProducts() { const products = await getProducts(); return <main className="px-6 py-12 md:px-12"><div className="flex items-end justify-between"><div><p className="eyebrow">Catalog</p><h1 className="mt-3 text-5xl">Products</h1></div><Link href="/admin/products/new" className="bg-[#211d18] px-4 py-3 sans text-xs font-bold tracking-[.12em] text-[#f4f0e8]">NEW PRODUCT</Link></div><div className="mt-10 overflow-x-auto border-y border-[#c8bfb2]"><table className="w-full min-w-[800px] text-left"><thead className="eyebrow"><tr><th className="py-4">Name</th><th>Origin</th><th>150g</th><th>300g</th><th>Stock</th><th>Status</th><th>Today</th><th>Delete</th></tr></thead><tbody>{products.map((product) => <tr className="border-t border-[#c8bfb2]" key={product.id}><td className="py-4"><Link href={`/admin/products/${product.id}`} className="underline">{product.name}</Link></td><td>{product.origin}</td><td>₩{product.price_150g.toLocaleString()}</td><td>₩{product.price_300g.toLocaleString()}</td><td>{product.stock_quantity}</td><td>{product.active ? "Active" : "Off"}</td><td>{product.todays_roast ? "Yes" : ""}</td><td><ProductDeleteButton id={product.id} /></td></tr>)}</tbody></table></div></main>; }