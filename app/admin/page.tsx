import Link from "next/link";
import { hasSupabaseEnv } from "@/lib/supabase/config";
import { createClient } from "@/lib/supabase/server";
async function isAdmin() { if (!hasSupabaseEnv) return false; const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return false; const { data } = await supabase.from("profiles").select("role").eq("id", user.id).single(); return data?.role === "admin"; }
export default async function Admin() { if (!(await isAdmin())) return <main className="px-6 py-24 text-center"><h1 className="text-5xl">Admin access required.</h1><Link href="/login" className="eyebrow mt-8 inline-block border-b border-[#211d18] pb-1">Sign in →</Link></main>; const supabase = await createClient(); const [{ count: products }, { count: orders }] = await Promise.all([supabase.from("products").select("id", { count: "exact", head: true }), supabase.from("orders").select("id", { count: "exact", head: true })]); return <main className="px-6 py-12 md:px-12"><p className="eyebrow">Overview</p><h1 className="mt-3 text-5xl">Dashboard</h1><div className="mt-12 grid gap-px border border-[#c8bfb2] sm:grid-cols-3"><div className="bg-[#f4f0e8] p-6"><p className="eyebrow">Total products</p><p className="mt-8 text-5xl">{products ?? 0}</p></div><div className="bg-[#f4f0e8] p-6"><p className="eyebrow">Total orders</p><p className="mt-8 text-5xl">{orders ?? 0}</p></div><div className="bg-[#f4f0e8] p-6"><p className="eyebrow">Today&apos;s roast</p><Link href="/admin/products" className="mt-8 block text-xl underline">Manage →</Link></div></div></main>; }