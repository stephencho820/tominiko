import { getProducts } from "@/services/products";
import { ProductCard } from "@/components/ProductCard";
export default async function Shop() { const products = await getProducts({ activeOnly: true }); return <main className="mx-auto max-w-[1440px] px-6 py-14 md:px-12 md:py-20"><div className="flex flex-col justify-between gap-8 border-b border-[var(--line)] pb-10 md:flex-row md:items-end"><div><p className="section-label mb-4">Tominiko Beans & Coffee</p><h1 className="text-6xl leading-none md:text-8xl"><span className="lang-ko">커피 선반</span><span className="lang-en">The coffee shelf</span></h1></div><div className="max-w-xs"><p className="sans text-sm leading-relaxed text-[var(--muted)]"><span className="lang-ko">매일 마시기 좋은 단맛과 선명함을 기준으로 고른 커피를 소개합니다.</span><span className="lang-en">A rotating selection of coffees chosen for sweetness, clarity, and the pleasure of drinking them every day.</span></p><p className="eyebrow mt-4 text-[var(--brown)]">{products.length} <span className="lang-ko">종류 / 서울</span><span className="lang-en">releases / Seoul</span></p></div></div>{products.length ? <div className="grid gap-x-8 gap-y-14 py-12 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => <ProductCard product={product} key={product.id} />)}</div> : <p className="py-20 text-2xl text-[var(--brown)]"><span className="lang-ko">현재 판매 중인 커피가 없습니다. 곧 새로운 배치가 도착합니다.</span><span className="lang-en">No coffees are currently available. New batches arrive soon.</span></p>}</main>; }